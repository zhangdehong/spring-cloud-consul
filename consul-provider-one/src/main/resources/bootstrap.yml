server:
  port: 5000

spring:
  application:
    name: hong-one-consul
  cloud:
    consul:
      host: 127.0.0.1
      port: 8500
      discovery:
        prefer-ip-address: true
        health-check-interval: 15s
        health-check-path: /health
        tags: hong,test